name: "JavaScript Action"
description: "Greet someone and record the time"
inputs:
  input-to-js: # id of input
    description: "Input to JS code"
    required: true
    default: "World"
runs:
  using: "node16"
  main: "./dist/index.js"
